{
	"name": "large-acres-backend",
	"version": "0.0.0",
	"private": true,
	"scripts": {
  "build": "tsc src/index.ts --outDir dist --module esnext --target es2020 --lib es2020,dom --esModuleInterop --skipLibCheck",
  "deploy": "wrangler deploy",
  "dev": "wrangler dev"
},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.0.7",
		"wrangler": "^4.19.1"
	}
}
